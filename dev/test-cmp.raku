my $a = 'a';
my $b = 1;
my $c = '1';
say "$a cmp $b: {$a cmp $b}";
say "$a cmp $c: {$a cmp $c}";

say "$b cmp $a: {$b cmp $a}";
say "$b cmp $c: {$b cmp $c}";


say "$a cmp $a: {$a cmp $a}";
say "$b cmp $b: {$b cmp $b}";

